<%= render 'standard/show_parent' if @parent_instance %>
<h1>New <%= @model.instance_name.capitalize %></h1>

<%= render 'standard/form', model: @model, instance: @instance, parent_instance: @parent_instance, remote: @remote %>

<%= link_to 'List', [@parent_instance, @model.collection_name], remote: @remote, class: 'btn btn-info' %>
